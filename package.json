{
  "name": "master-scheduler-dashboard-srm",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://thebardchat.github.io/MASTER-Scheduler-Dashboard-SRM",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:gh": "GITHUB_PAGES=1 vite build",
    "preview": "vite preview",
    "deploy": "GITHUB_PAGES=1 npm run build && gh-pages -d dist",
    "deploy:cf": "npm run build && wrangler pages deploy dist --project-name=srm-dispatch"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "gh-pages": "^6.3.0",
    "vite": "^5.1.0",
    "vite-plugin-pwa": "^1.2.0"
  }
}
